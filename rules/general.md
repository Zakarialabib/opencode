# General Rules

- Always prioritize code readability and maintainability.
- Provide inline comments for complex logic.
- Follow a test-driven approach when possible.
- Avoid introducing unnecessary dependencies.
- Ensure all commands are executed with correct permissions.
- Format code before saving it (use the configured formatters).
- Respect the existing project structure and conventions.