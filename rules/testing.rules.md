# Testing Rules

- Tests must cover the changed behavior, not only the happy path.
- Release coverage target is 80% unless an exception is approved.
- Bug fixes require a regression test or a written reason why automation is not practical.
- Report commands actually run and their result.
- Treat skipped, flaky, or not-run tests as release risks.
